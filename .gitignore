# Datasets and pipeline outputs (downloaded / generated by the user)
data/
sam3d_output/
output/
visualization/

# Model weights (downloaded during setup)
sam3d/checkpoints/
lift4d_scgs/extern/ldm_zero123/*.ckpt

# SAM3 (stage 1) is pip-installed; its weights live under sam3/ — never commit
sam3/

# Python / build
__pycache__/
*.pyc
*.egg-info/
build/
*.so

# Local setup/test scratch (from the release refactor; not part of the source)
.build_env*.sh
.build_env*.log
.download_*.sh
.download_*.log
.test_*.sh
.test_*.log
.run_*
.job_*
.jobq/
.backfill_*
.clone_*
.davis_dl*
.extract_*
.install_*
.sam3_weights*

# Excluded from the public release
sam3d/notebook/
tests/

# demo videos (download separately)
*.mp4
